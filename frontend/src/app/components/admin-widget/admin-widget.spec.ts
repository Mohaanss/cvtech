import { AdminWidgetComponent } from './admin-widget';
describe('AdminWidgetComponent', () => {
  it('should create the admin widget component', () => {
    const component = new AdminWidgetComponent();
    expect(component).toBeTruthy();
  });
});
